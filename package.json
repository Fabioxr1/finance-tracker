{
  "name": "finance-tracker-tests",
  "version": "1.0.0",
  "description": "Unit tests for Finance Tracker",
  "scripts": {
    "test": "vitest run"
  },
  "devDependencies": {
    "vitest": "^1.6.0"
  }
}
